import styled from 'styled-components';

export const Select = styled.select`
  min-height: 40px;
  font-size: 1.3rem;
  border-radius: 10px;
  border: 1px solid #ccc;
  padding: 10px 20px;
  appearance: none;

  &:focus{
    outline: none;
  }
`;
